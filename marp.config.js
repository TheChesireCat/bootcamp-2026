// Marp CLI config — registers the NetSI theme and enables local images.
module.exports = {
  allowLocalFiles: true,
  themeSet: './themes',
  options: {
    // default HTML output; use --pdf / --pptx flags or the npm scripts to switch
  },
};

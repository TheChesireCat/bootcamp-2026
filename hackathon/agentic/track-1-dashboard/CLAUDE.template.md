# CLAUDE.md (starter, edit me)

Copy this to `CLAUDE.md` in your project folder. The agent reads it every session.

## Project
- A single-page static website that visualizes a network from `network.json`.
- Plain HTML + JS, no build step. Libraries via CDN only (e.g. vis-network or D3).

## How to run
- Open `index.html` in a browser, or `python3 -m http.server` then visit the printed URL.

## Conventions
- Small changes. Show me a plan before writing or restructuring the whole page.
- Keep it to one `index.html` (inline CSS/JS is fine) unless I ask to split files.
- After any change, tell me exactly how to view it, and I will check it in the browser.
- Don't add a package.json / build tooling.
